export class Music {
    id: number;
    title: string
    gender: string

    constructor(id: number, title : string, gender : string){
        this.id = id
        this.title = title
        this.gender = gender
    }
}